# PROJECT_STATE.md — TrueResearch Investment Platform

*Read this at the start of every new chat. Last updated: Session 11, part 2 — the Session 10 part 9 model sanity-check concern (IDEA scoring 98 "Strong" despite -96.6% ROE) is now FIXED via a distress guardrail in `14_score_current_stocks.py` (see entry below). Next priority: the `shareholding_pattern` data-source decision (Session 10 part 10), then the ASTRAL price-fetch failure.*

## Session 11, part 2 — TrueScore distress guardrail added (fixes the Session 10 part 9 sanity-check failure)

**Root cause found:** TrueScore is 50% relative valuation (sector-relative P/E percentile) + 50% an ML model's predicted 3-month excess return. Neither component directly checks solvency — ROE is only one of 14 raw inputs to the ML model, which was trained/validated purely to predict price momentum, never tested for distress avoidance (confirmed by re-reading `TrueScore_Validation_Report.md`'s own caveats — a distress check isn't among them). Worse, when a company has **negative shareholders' equity**, ratios like ROE and P/E divide by a negative number, which can distort or flip their apparent meaning, letting a genuinely distressed company look deceptively OK on paper. `PRD.md` B2 had already written the required rule ("a company in default or clearly distressed must not score in the 80s+") but it had never actually been implemented in code — only caught manually, after the fact, by Avdhoot spotting IDEA on the Stock Detail Page.

**Fix shipped:** added a guardrail directly in `14_score_current_stocks.py`. A stock is flagged "financially distressed" if it has negative shareholders' equity OR an ROE worse than -50%. Flagged stocks have their combined `overall_score` capped at 15 (forcing them into the existing "Weak" band) regardless of what the valuation/ML components computed — no new rating label introduced, so the frontend needed zero changes. Deliberately simple/conservative (not a new ML model), and the raw component scores (`relative_valuation_score`, `ml_rank_score`) are left untouched in the database for transparency/audit — only the combined score and rating are overridden.

**Re-ran `14_score_current_stocks.py` after the fix.** Result: **498/500 scored** (ICICIAMC and MEESHO still skipped — insufficient price history, same known/expected gap as Session 8, not new). **4 stocks flagged by the new guardrail: GMRAIRPORT, IDEA, OLAELEC, TTML** — all now correctly capped at 15/"Weak". Notably, OLAELEC and TTML were not previously caught by manual review — this guardrail found 2 additional real cases beyond the one Avdhoot originally spotted. New Top 10 by overall score confirmed clean of any distressed names (INDIAMART, FINCABLES, FORCEMOT, ERIS, NTPC, NLCINDIA, NMDC, VEDL, LUPIN, SUZLON).

**Next immediate step:** the `shareholding_pattern` data-source decision (Session 10 part 10) is now the top open item, followed by the ASTRAL price-fetch failure (Session 11 part 1) and the Screener follow-ups (multi-select compare, preset filters).

## Session 11, part 1 — closed out Session 10's open item: price refresh confirmed + everything pushed to GitHub

**Price refresh confirmed complete.** Re-ran `venv\Scripts\python.exe 05_daily_price_refresh.py --force` (the run Session 10 part 13 kicked off but never confirmed finished). Result: **499/500 stocks now have live prices.** Only `ASTRAL` failed — flagged as a small follow-up (likely a ticker-symbol mismatch between the data source and our `assets` table for this one stock), not urgent, not investigated yet.

**Both repos pushed to GitHub, fully caught up:**
- `TrueResearch Code` (pipeline repo): commit `71c1f51` — `23_add_public_read_policies.sql`, `24_grant_anon_select.sql` (shareholding_pattern additions), `PROJECT_STATE.md`, and `Claude outputs/Design_System.md` + `Claude outputs/Session_Log_Phase_C_Permissions_and_Prices.md`. (Two stray duplicate files, `23_add_public_read_policies-1.sql` and `24_grant_anon_select-1.sql`, were deliberately left uncommitted — they look like accidental duplicate saves; safe to delete manually in File Explorer whenever convenient.)
- `trueresearch-frontend`: commit `8cf7165` — the Screener page, Stock Detail page, the disabled `debug-schema` page, and all Session 10 components (`AssetCard`, `ChangePill`, `DisclaimerBar`, `Footer`, `InfoTip`, `LearnCallout`, `Nav`, `PlaceholderPanel`, `PriceChart`, `ScoreBadge`, `ScreenerTable`, `SectorChip`, `ShareholdingChart`, `Sparkline`, `icons`) were all previously local-only; now on GitHub.

**Confirmed the scheduled GitHub Actions workflow (`daily-prices.yml`) is already correctly configured** — weekday cron runs at market open, every 15 min through the trading day, and at close (IST) — and since it wasn't in the uncommitted-changes list, it was already live on GitHub before this session; this session's push doesn't change its behavior, just backs up the rest of the code alongside it. This should prevent the "300 new stocks never got a first price fetch" gap from recurring.

**Next immediate step:** unchanged from Session 10 part 9/13 — the model sanity-check concern (IDEA scoring 98/100 "Strong" despite -96.6% ROE and negative book value) is the top open priority, followed by the `shareholding_pattern` data-source decision (part 10) and the ASTRAL price-fetch failure above.

**`trueresearch-frontend` is now on GitHub too:** `https://github.com/sp2591996/trueresearch-frontend` (private) — separate repo from `truresearch-pipeline` (the Python pipeline's repo). First push done in Session 9 (`git remote add origin` + `git push -u origin main`); future sessions just need plain `git push` from inside that folder, same as the pipeline repo.

## Session 10, part 13 — frontend permissions fix (whole-site "no data" bug) + live price gap found

**Problem:** the frontend (`localhost:3000`) was showing no data anywhere — Screener and other pages appeared blank. Root cause was the same two-gate Postgres/Supabase permission issue flagged for `shareholding_pattern` back in part 10, except this time it was blocking every table, site-wide: Row Level Security was enabled (Session 4) but the `anon` role (what the frontend's publishable key connects as) had neither (1) an RLS read policy nor (2) a table-level `GRANT SELECT` — `service_role` (the Python pipeline) had both, `anon` had neither, because the frontend didn't exist when that was set up.

**Fix:** re-ran `23_add_public_read_policies.sql` and `24_grant_anon_select.sql` (both already covered the 12-table list from part 10) in Supabase's SQL Editor. Confirmed working by opening the actual Screener page in a browser and seeing all 500 stocks render with tickers/sectors/scores/P/E/market cap.

**Second, separate issue found right after:** most rows on the Screener showed `—` for price and a "Markets closed" pill instead of a real day-change % — 291 of 500 stocks, confirmed by counting rows on the live page. This is a **data gap, not a permissions gap**: `19_add_nifty500_expansion.py` (which added 300 stocks to reach the full Nifty 500) explicitly never fetched any price data for them — that was left as a manual "Step 3 onward," which was never run.

Checked both price scripts to isolate exactly what was missing:
- `20_backfill_new_stocks_price_history.py` (10-year historical backfill into `prices_daily`, for chart data) — **run, and found all 500 stocks already had history.** Nothing was actually missing here; this part had been done already in an earlier session.
- `05_daily_price_refresh.py` (fetches *today's* live price into `live_prices`) — this is the actual gap. It also **skips itself entirely outside NSE market hours (Mon–Fri 9:15–15:30 IST) unless `--force` is passed**, and this session ran on a Sunday, so it had to be force-run: `venv\Scripts\python.exe 05_daily_price_refresh.py --force`. **Status: this was kicked off but not yet confirmed complete as of this write-up — check its final `Done. X/500 ok.` line and any `Failed:` tickers next session, and re-check the Screener page for remaining "Markets closed" rows.**

**Environment note for future sessions:** on Avdhoot's Windows laptop, PowerShell blocks `.ps1` venv activation by default (`running scripts is disabled on this system`) and the `.bat` version doesn't persist in PowerShell either (it activates a throwaway sub-process). Simplest fix, works every time: skip activation entirely and call the venv's Python directly —
```
venv\Scripts\python.exe <script_name>.py
```

**Next immediate step:** confirm the `--force` price refresh completed cleanly and the Screener shows live prices for (near-)all 500 stocks. After that, worth checking whether the scheduled GitHub Actions job for daily prices (`.github/workflows/daily-prices.yml`) is actually enabled/running on schedule, so this same gap doesn't recur the next time new stocks get added. The Session 10 part 9 model sanity-check item (IDEA scoring 98 "Strong" despite -96.6% ROE) is unrelated and still fully open.

## Session 10, part 10 — Stock Detail Page polish + Screener page built
Three things Avdhoot asked for, in order:

1. **Tooltip content and style fixed.** The "(i)" info icons added in part 9 had two real problems: the wording leaked internal jargon (e.g. "reported in the ratios snapshot"), and they used the browser's own native tooltip (a plain OS box) which clashed with the dark violet theme. Checked the old ClearStocks site's live `dashboard_data.json` for its own Verdict tooltip wording/style as a reference point. Fixed: `RATIO_INFO` definitions rewritten in plain, professional language; `InfoTip.tsx` now renders a small themed tooltip card (bg-tr-card-2, bordered) instead of the native one.
2. **TrueScore rating badge fixed again.** The part 9 fix (`hideValue`) dropped the duplicate number but kept the little conic-gradient ring dial next to the rating word, which still looked like a second, meaningless progress ring. `ScoreBadge`'s `variant="rating"` now renders as a plain solid pill (no ring at all) tinted to the score's red/amber/gold/green band. **Regression caught and fixed in the same pass:** the default (non-rating) variant had also stopped rendering its `label` prop, which silently broke the rating text on `AssetCard` (used on the Home page's "Top Rated" list and now the Screener table too) — restored.
3. **TrueScore card's empty space filled with a real "vs sector peers" valuation verdict** (Avdhoot picked this over two simpler options). Modeled directly on the old ClearStocks site's own Verdict column — pulled its live `dashboard_data.json` to confirm the actual logic rather than guessing: P/E vs the sector's median P/E (falls back to P/B when a stock's own P/E isn't usable, e.g. loss-making companies), ±20% band for Cheap/Fairly valued/Expensive. Needs at least 5 same-sector peers with a usable ratio, or the verdict is omitted rather than shown on a shaky base. New sector-peers query added to the Stock Detail Page (one query via `assets!inner(sector_id)`, not N+1).

**`shareholding_pattern` permissions fixed, but a bigger gap found underneath.** `23_add_public_read_policies.sql` and `24_grant_anon_select.sql` were updated to include this table and Avdhoot ran both in Supabase's SQL Editor — confirmed working (queried the table directly via its REST endpoint with the anon key: `200 OK`, no more "permission denied"). **But the table itself is completely empty — `0` rows, for every stock, not just one** (confirmed via `Prefer: count=exact`). Checked every pipeline script (`01`–`24`) and none of them ever ingest shareholding data — there is no `0X_shareholding_refresh.py` or equivalent. So this was never actually a permissions-only gap as Session 10 part 8 assumed; it's a missing data source, same class of gap as mutual funds (J3) and gold. **Corrected the earlier assumption here rather than leaving it wrong.** `components/ShareholdingChart.tsx` (built this session, renders 4 quarters as stacked bars) is real, tested code — it's just never had real data to render yet. The Stock Detail Page correctly falls back to the placeholder until an ingestion script exists for this table.

**Open item, corrected:** `shareholding_pattern` needs (1) a data source decision -- likely NSE/BSE corporate filings or a paid data vendor, unlike price/fundamentals which came from the existing `market_data_provider.py` -- and (2) a new ingestion script, before this section can show anything real. Not just "run the SQL" as previously stated.

**Screener page built** (`app/screener/page.tsx` + `components/ScreenerTable.tsx`) — the next page in Session 9's build order (Home → Stock Detail Page → Screener → rest). L1 scope only, per PRD.md C4's own note that L1 just needs "the standard filter set from C1": search by ticker/name, sector filter, minimum-TrueScore filter, every column sortable (client-side, all ~500 rows fetched once). **Deliberately not built this pass** (separate PRD items): checkbox multi-select into the C3 comparison page, named filter presets (C2), sector-vs-sector aggregate mode, and the Equities/Mutual Funds/Gold asset-type toggle (Mutual Funds/Gold data isn't live yet anyway). `scores`/`ratios_snapshot` are point-in-time snapshot tables, so this page dedupes to each stock's single latest row from both, same pattern as the sector-verdict query above.

**Next immediate step:** Avdhoot's explicit call — come back to the Session 10 part 9 model sanity-check concern (IDEA scoring 98 "Strong" despite -96.6% ROE and negative book value) before trusting `truescore_v2` output more broadly. The newly-found shareholding data-source gap and Screener's own next layer (checkbox compare, presets) are secondary to that.

## Session 10, part 2 — rebrand to match ClearStocks
After seeing the Navy & Gold Home page (part 1, directly below), Avdhoot asked to switch the whole site's look to match his older ClearStocks site (`https://sp2591996.github.io/clearstocks-website/`) instead. Confirmed explicitly before proceeding (this overrides a previously "FULLY DECIDED" branding item, so it got a direct yes/no check, not a silent swap — see `PROJECT_STATE.md`'s own working-protocol rule on this).

**What changed:** pulled the exact color tokens and fonts straight from the live ClearStocks site's stylesheet (not re-guessed) — dark background (`#0A0A12`), card surfaces (`#131320`/`#1C1C2E`), a violet-to-pink gradient accent (`#8B5CF6` → `#EC4899`) replacing gold, green/red for gains/losses, Manrope for headings (replacing Merriweather), Inter for body text, IBM Plex Mono for numbers/tickers. Full token table and rationale now in `Design_System.md` Section 2b — **that section is the new locked palette; Section 2 (Navy & Gold) is marked superseded, not deleted, so the history is still there.**

**Every component from part 1 was restyled to match, not rebuilt from scratch:** `Nav.tsx` (sticky header, gradient underline strip, gradient logo mark, pill search box), `Footer.tsx`, `DisclaimerBar.tsx` (now amber/gold notice-style), `LearnCallout.tsx` (now the "info-dot + tooltip" pattern from the old site), `PlaceholderPanel.tsx`, `ScoreBadge.tsx` (now a small conic-gradient dial, matching the old site's score dial instead of a plain bordered circle), `AssetCard.tsx`, and the Home page (`app/page.tsx` — same block order as before, now with the eyebrow tag / gradient hero touches from the old site). `app/globals.css` and `app/layout.tsx` hold the new design tokens and fonts.

**Single dark theme now, not light+dark.** The old ClearStocks site never had a light mode, so unlike the original Navy & Gold plan (which defined both), this palette is dark-only for now — flagged in Design_System.md Section 5 as a rule change, in case a light mode ever gets requested later.

**Design_System.md was rewritten to match** (not just this file) — Section 2 marked superseded with history kept, new Section 2b with the full token table pulled from the live site, Section 3 (typography) and Section 4/5 updated to the new fonts/rules. Saved into `TrueResearch Code/Claude outputs/Design_System.md` (that folder already held `PRD.md`/`Wireframes.md` on Avdhoot's computer — Design_System.md had never been saved there before this session, only shared inside chat).

**Not done yet:** the actual `clearstocks-website` GitHub repo/site itself is untouched — this only copied its look into TrueResearch. Sector Chip, Comparison Table, and Chart components (also part of the old site's system) are still not built — they'll get built once a page actually needs them.

**Next immediate step:** unchanged from part 1 — build the **Stock Detail Page** next, reusing all the now-restyled components.

## Session 10, part 9 — IMPORTANT: possible TrueScore model sanity-check failure found
While reviewing the new Stock Detail Page, Avdhoot pulled up IDEA (Vodafone Idea) and it exposed something more important than a display bug: **IDEA scores 98/100, "Strong," under the real, validated `truescore_v2` model** — the same model signed off in Session 6 — **while its ROE is -96.6% and its P/B is negative** (negative book value: liabilities exceed assets). `PRD.md` B2 states explicitly, as a required edge case: *"a company in default or clearly distressed must not score in the 80s+ — this is a required manual sanity check before every model change ships."* A stock with deeply negative ROE and negative book value is about as clear a distress signal as exists. This looks like exactly the failure mode that rule exists to catch.

**This is flagged, not fixed** — a frontend session can't responsibly patch a scoring model. Two things were done:
1. The Stock Detail Page now surfaces this automatically: if a stock scores 80+ while showing very negative ROE or negative P/B, an amber "Model sanity-check flag" banner appears directly on its page, quoting the actual numbers and pointing at PRD.md B2's rule. This isn't IDEA-specific — it'll trigger on any stock in the same situation, which itself is useful for spotting how widespread this might be.
2. This needs a real look at the model/data, not a UI tweak: possible causes worth checking — (a) the model may be over-weighting `relative_valuation_score` (IDEA's was 100 — pure momentum/valuation percentile can rank a beaten-down, high-risk stock at the top if it's cheap purely on price, with nothing accounting for solvency), (b) a data quality issue in `fundamentals`/`ratios_snapshot` for this stock specifically, or (c) the sanity check Session 6 signed off on may have been checked against the original 200-stock universe and never re-verified stock-by-stock after Nifty 500 expansion (Session 8) or the sector-neutral re-validation (Session 9) — neither of those explicitly re-ran the "no distressed stock scores 80+" check as its own test. **Recommend Avdhoot decide whether this needs a dedicated check script (similar to `21_sector_neutral_robustness_check.py`) before trusting `truescore_v2` output broadly**, especially before any real user sees a "Strong" rating on a stock this financially distressed.

**Same pass also fixed 3 real frontend defects on the same page** (not just the model concern): (1) Financial health/Valuation used a fixed 2-column grid with one tile per stat, so a stat with no data left a visible empty gap instead of the grid collapsing — same "looks broken" pattern as the Home page's curated lists earlier. Replaced with `StatRow`, which builds a list of only the stats that actually have data first, then renders that in a flowing row (or a placeholder if none exist). (2) The TrueScore section rendered two side-by-side badges showing the exact same number (`overall_score` twice, styled differently) — the schema has no separate "TrueScore Rating" number, just a text label on the same score, so showing the number twice read as a duplicate-rendering bug. Collapsed to one badge with the rating as its label (matches the Home page's `AssetCard` pattern). (3) Negative P/E and P/B rendered as literal negative numbers ("-4.55"), which looks like a data error rather than what it means (negative earnings/book value) — now shown as "N/M" (not meaningful), the standard finance-site convention, with a one-line legend explaining it.

## Session 10, part 8 — Stock Detail Page built
The page Avdhoot asked to build next. Built at `app/stocks/[ticker]/page.tsx` (e.g. `/stocks/IDEA`) — the "Top Rated" list's `AssetCard` links already pointed at this exact route, so clicking a stock from Home now works.

**Checked real column names first, via a temporary `/debug-schema` page, rather than guessing** (guessing was the reason `AssetCard`/curated-list bugs happened earlier). That check surfaced real gaps between what `PRD.md`/`Wireframes.md` describe and what's actually in the database — this page is honest about those gaps rather than faking them:
- **TrueScore only has 2 real components**, not the 5 (Quality/Growth/Valuation/Momentum/Risk) `PRD.md` B2 describes — `scores` has dedicated `relative_valuation_score` and `ml_rank_score` columns only, matching Session 4/5's own build notes. The page shows those 2 real numbers and says so in the UI ("TrueScore currently blends two signals... more factors are planned").
- **`peers` has zero rows for every stock** (confirmed, not assumed) — Peers section is a placeholder, not a fake "no peers found" empty state.
- **`shareholding_pattern` returned "permission denied"** — no RLS/read policy exists for it yet (same class of gap Session 9 fixed for `scores`/`assets`/etc. via `23_add_public_read_policies.sql`/`24_grant_anon_select.sql` — this table just never got added to those two scripts). Placeholder for now; flagged as an open item below.
- Mutual fund exposure, corporate actions, insider activity, and written research are placeholders per `Wireframes.md` itself (already spec'd that way, not a shortcut taken here).

**What's real on this page:** header (price, day change, sector, ticker/name) from `live_prices`/`assets`/`sectors`; TrueScore + its 2 real components; financial health (margin, debt/equity, ROCE, ROE) and valuation (P/E, P/B, EV/EBITDA, P/S) from `ratios_snapshot`/`fundamentals`, each stat hidden (not shown as "N/A") when the underlying value is null, per PRD.md's own edge-case rule; a price history section with a plain SVG sparkline (no charting library added) plus 1D/1W/1M computed returns from `prices_daily`.

**New components:** `ChangePill.tsx` (the +/-% pill, color + icon together per the accessibility rule), `SectorChip.tsx`, `Sparkline.tsx` (a lightweight server-rendered SVG line chart — not a full interactive 1M/6M/1Y/5Y chart, that's a tracked follow-up). Added several icons to `icons.tsx` (trending-down, star, users, shield, newspaper).

**Cleanup:** the temporary `/debug-schema` page (used only to check real column names) has been disabled — it now just 404s. Claude can't delete files on Avdhoot's computer directly; the folder `trueresearch-frontend/app/debug-schema` can be deleted manually in File Explorer/VS Code whenever convenient, it isn't linked from anywhere in the site.

**Open item, new:** `shareholding_pattern` needs a public read policy + anon grant (same pattern as `23_add_public_read_policies.sql`/`24_grant_anon_select.sql` from Session 9) before its Stock Detail Page section can go live — currently a placeholder for a permissions reason, not a missing-feature reason.

**Next immediate step:** Screener page (PRD.md C1/Wireframes.md), per the build order Session 9 set — Home, then Stock Detail Page (done), then Screener, then the rest.

## Session 10, part 7 — disclaimer bar: duplicate + width fix
Two small but real bugs from one screenshot: (1) the disclaimer bar was appearing TWICE on the Home page — once placed directly in `app/page.tsx`, once again inside `Footer.tsx` (which is already on every page via `layout.tsx`). Removed the extra copy from `app/page.tsx`; Footer's is the only one now, and the "one per page" expectation is written into `DisclaimerBar.tsx`'s own comment so it doesn't get re-added by accident later. (2) its text ran fully edge-to-edge while every other section sits inside the shared 1320px container, so it visibly didn't line up with the nav/hero/curated lists above and below it. Fixed by keeping the bar's background full-bleed (a full-width strip still reads fine) but wrapping its text in the same `max-w-[1320px]` container as everything else.

## Session 10, part 6 — layout bugs + real hero banner + real logo
Avdhoot flagged concrete problems with part 5's result (not taste this time — actual defects), point by point:

1. **A real nav bug I introduced in part 5:** the nav row had two `flex-1` elements (the links `<nav>` AND a leftover spacer `<div>`) fighting for the same space, so the links only got half the room they needed — they visibly overflowed into a browser scrollbar and clipped "Learn"/"My TrueResearch" off entirely. The spacer is deleted; `<nav>` is the only `flex-1` now, so it gets all the free space. This was a straightforward mistake, not a design-taste issue — worth remembering not to leave two `flex-1` siblings in the same row again.
2. **The content container was too narrow.** `max-w-6xl` (1152px) was leaving large empty gutters on normal desktop screens — the 4 curated-list columns really were only using ~60% of the browser width, exactly as reported. Widened to `max-w-[1320px]` everywhere (Nav, Footer, every Home section) — this exact number matches the old ClearStocks site's own container width, not an arbitrary guess.
3. **The persona-quiz "coming soon" card was removed** from the Home page. It was sitting as the very first thing under the hero, full width, with the same visual weight as real content — but Wireframes.md describes it as a first-visit, dismissible *prompt*, not a permanent section. Showing a permanent "Planned" placeholder there both looked bad and didn't match the actual spec. It'll come back once the real quiz exists.
4. **Market Movers and Market Mood** were two full-width placeholder panels stacked one on top of the other — pure vertical stacking that wasted the (now wider) horizontal space. Placed side by side in a 2-column row.
5. **Added a real hero banner treatment** — soft blurred violet/pink gradient shapes behind the headline (pure CSS, no image file needed), so the hero reads as a designed banner instead of flat centered text on a flat background.
6. **Built an actual logo mark.** The "TR" in the nav was plain white initials in a gradient square, not the interlocked T/R monogram Design_System.md Sections 1 and 2b actually describe. New `LogoMark` in `components/icons.tsx` draws that interlocked-T/R shape as real SVG geometry. **Checked first and confirmed: no logo file (svg/png/jpg) exists anywhere in the project folder** — only the default Next.js starter icons in `trueresearch-frontend/public/`. So this is a fresh drawing of the documented concept, not a reused asset. If Avdhoot has an actual logo file from an earlier chat/session, it should replace this drawing directly rather than being redrawn again.

**Process note for future sessions:** before showing a UI change, actually reason through the CSS/flex behavior (e.g. "does anything else in this row also claim flex-1?") rather than shipping something that looks plausible in the code and letting Avdhoot's screenshot catch layout math errors. That's what happened in part 5's nav.

**Next immediate step:** unchanged — build the **Stock Detail Page** next.

## Session 10, part 3 — professional visual pass
Avdhoot's feedback on part 2's dark/violet restyle: "make the design professional to look." Rather than guess at specifics, made a set of concrete, standard polish changes real finance-product UIs use, on top of the same dark/violet palette (this did not change colors again, just how they're used):

- **New `components/icons.tsx`** — a small inline SVG icon set (search, info, close, compass, trending-up, gauge, clock, alert, book). Replaces every emoji used across Nav, Home, DisclaimerBar, LearnCallout, and PlaceholderPanel — emoji render inconsistently across operating systems and read as informal rather than a finance product.
- **`DisclaimerBar.tsx`** — was amber/"alert"-styled (matching the old ClearStocks site's warning-banner look); toned down to a quiet neutral strip with an info icon. Amber/red reads as "something's wrong," which isn't the right tone for routine compliance text that sits on every page. Amber is now reserved for genuinely experimental/unvalidated cases only.
- **`PlaceholderPanel.tsx`** — was a dashed-border box (read like a wireframe sketch); now a solid-bordered card with a small icon badge and a quiet "Planned" tag, closer to how real products show a not-yet-built section as a stated fact rather than an alarm.
- **`ScoreBadge.tsx`** — the TrueScore Rating variant's dashed outline (looked "unfinished") replaced with a thin solid accent ring — still visually distinct from plain TrueScore, without looking unpolished.
- **`Nav.tsx`** — thinner gradient strip, real search icon, no gradient hover on plain nav links (gradient now reserved for the one primary action, Log in, so it keeps its visual weight instead of being sprinkled everywhere).
- **`AssetCard.tsx`** and the Home page's filter chips — dropped the colorful glow-shadow / gradient-on-hover effects in favor of plain border-color changes, matching a single restrained hover style site-wide.
- **`globals.css`** — added one consistent focus-ring style used by every interactive element, plus slightly tighter letter-spacing/antialiasing for a cleaner text render.

**Next immediate step:** unchanged — build the **Stock Detail Page** next.

## Session 10, part 1 — site-wide components + Home page (Navy & Gold, later restyled — see parts 2/3 above)
Continues straight on from Session 9's "Next immediate step": build the two site-wide components Wireframes.md flags as needed before any individual page, plus basic layout/nav, then start the Home page.

**New components (in `trueresearch-frontend/components/`), all styled from the locked Navy & Gold palette (`Design_System.md` Section 2) via CSS tokens added to `app/globals.css` (`--tr-navy`/`--tr-gold`/etc., light + dark mode both defined):**
- `DisclaimerBar.tsx` — the required disclaimer, dismissible for the current visit only (per spec: "dismissible-per-session but never permanently hidden" — it comes back on the next page load). Used in the Footer (so it's on every page) and again inline on the Home page.
- `LearnCallout.tsx` — the "?" icon → inline plain-English explainer + link to the full Learn article, ready to attach to any jargon term once real Learn/glossary pages exist.
- `PlaceholderPanel.tsx` — the one reusable "coming soon" component Wireframes.md calls for (used everywhere instead of one-off designs per feature); requires a one-sentence `detail` prop explaining what's coming, so a page can't accidentally ship a placeholder that says nothing (PRD.md's placeholder rule).
- `ScoreBadge.tsx` and `AssetCard.tsx` — also from Design_System.md's component table, built now because the Home page's Top Rated list needed them; every score badge uses the same red→amber→green band and never relies on color alone (rating label always shown as text too).
- `Nav.tsx` and `Footer.tsx` — site-wide top nav (Home / Screener / Research / IPOs / Learn / My TrueResearch, per the Session 3 nav change) and footer (disclaimer bar + Learn/About/sector links). Both now wired into `app/layout.tsx` so every page gets them automatically — no per-page work needed.

**Home page rebuilt** (`app/page.tsx`, replacing the default create-next-app starter page) following `Wireframes.md`'s exact block order: hero band, persona-quiz-prompt placeholder, curated lists row, Market Movers placeholder, Market Mood placeholder, Learn teaser. **Being honest about what's real vs. placeholder:** the "Top Rated" curated list is REAL — it queries live TrueScore data from Supabase (same proven query as `test-connection`, just formatted with the new `AssetCard`/`ScoreBadge` components). The other 3 curated list types (Undervalued/High Growth/High Quality), Market Movers, Market Mood, and the persona quiz all show a `PlaceholderPanel` instead of fake data, because their underlying config tables or jobs (list configs, live day-change feed, the sentiment formula) don't exist yet — building those is separate, later work, not silently faked here.

**Heading font added:** Merriweather (a serif, matching Design_System.md Section 3's "classic finance feel" recommendation for the Navy & Gold direction) via `next/font/google`, same pattern as the existing Geist fonts — wired up as `--font-heading` / the `font-heading` Tailwind class.

**Not done yet, flagged deliberately:** the actual Stock Detail Page, Screener, and every other nav link (`/screener`, `/research`, `/ipos`, `/learn`, `/my-trueresearch`, `/login`, `/about`, `/stocks/[ticker]`) don't exist yet — the Nav/Footer/Home page link to them already so the site's structure is visible, but clicking them will 404 until each page is built. This is expected at this stage, not a bug.

**How to see this (for Avdhoot, non-coder — exact steps):** 1) Open the `trueresearch-frontend` folder in VS Code. 2) Open a Terminal (Terminal menu → New Terminal). 3) Type `npm run dev` and press Enter. 4) Open a browser to `http://localhost:3000`. (Note: by the time you're reading this, the look has changed again — see "Session 10, part 2" above. The steps to view it are identical; only the colors/fonts changed.)

**Not pushed to GitHub yet** — same pattern as other sessions: these new files exist locally in `trueresearch-frontend` and work, but need `git add . && git commit -m "..." && git push` from inside that folder to back them up to the repo.

**Next immediate step:** per the Session 9 plan — build pages in order: **Stock Detail Page next** (most important single page per PRD.md B1/Wireframes.md), then Screener, then the rest. The new `AssetCard`/`ScoreBadge`/`PlaceholderPanel`/`DisclaimerBar`/`LearnCallout` components should all be reused there, not rebuilt.

## Session 9 — frontend connected to Supabase
Continues straight on from "Session 9 — frontend build begins" above (same session).

**What was built:** `lib/supabaseClient.ts` (the one file that creates the Supabase connection frontend-side — same role as `db_client.py` on the Python side, uses the PUBLISHABLE/anon key only, never the secret key) and a temporary `app/test-connection/page.tsx` that pulls the top 5 highest-scoring stocks straight from the live `scores`+`assets`+`sectors` tables. Both written directly into `trueresearch-frontend` (granted folder access this session).

**Real permissions gap found and fixed — worth remembering for any future table added to the frontend.** Confirming this connection took 3 attempts, each surfacing a different, real layer:
1. **Invalid API key** — the Supabase "Publishable key" display is truncated on-screen (ends in "..."); manually selecting the visible text instead of using the copy-icon button copies a truncated, invalid key. Fix: always use the copy icon, never select-by-hand, for this key.
2. **Row Level Security blocked all reads** — RLS was enabled on every table back in Session 4 with no read policies ever added (deliberate, so nothing was public by accident) — meaning correct keys still return zero rows or a permission error until policies exist. Fixed via new `23_add_public_read_policies.sql` (adds `FOR SELECT USING (true)` policies — read-only, no write access granted).
3. **Table-level GRANT missing (Postgres error 42501, separate from RLS)** — even after RLS policies existed, the `anon` database role itself was never granted basic SELECT access to any table (Session 4 only ever granted `service_role`, since the frontend didn't exist yet). Fixed via new `24_grant_anon_select.sql` (`GRANT USAGE ON SCHEMA public` + `GRANT SELECT` per table, to `anon` and `authenticated` roles).

**Both scripts only grant READ access** — the frontend's anon/publishable key still cannot insert, update, or delete anything; all writes still only happen from the Python pipeline via the separate secret/service_role key, which never reaches the browser.

**Tables covered so far** (in both new SQL scripts): `assets`, `sectors`, `scores`, `score_components`, `score_backtest_results`, `score_performance_tracking`, `ratios_snapshot`, `fundamentals`, `prices_daily`, `live_prices`, `peers`. **Add more tables to both scripts as the build reaches pages needing them** (e.g. `research_reports`, `ipos`, `learn_content`, `articles`, `mutual_fund_nav_history` once J3 data exists, etc.) — this is meant to be extended, not treated as a one-time complete list.

**Confirmed working:** `http://localhost:3000/test-connection` renders a real table of 5 live stocks (IDEA 98.4/Strong, INDIAMART 95.8/Strong, FINCABLES 92.9/Strong, FORCEMOT 91.0/Strong, ERIS 89.8/Strong — run_date 2026-09-06) pulled directly from Supabase.

**Next immediate step:** per the plan from earlier this session — build the two site-wide components Wireframes.md flags as needed before any individual page (Disclaimer Bar, Learn Callout) plus basic layout/nav, then start on the Home page.

## Session 9 — frontend build begins
Avdhoot chose the frontend build (over scaling qualitative research content, deferred to later) as Phase C's next focus, after all 3 PRD open decisions were resolved (see below).

**Local environment set up on Avdhoot's computer (non-coder, needed full hand-holding — record this so a future session knows what's already done and doesn't re-explain from scratch):**
- Node.js v24.20.0 and npm 11.19.0 installed (were not present before this session).
- Windows PowerShell execution-policy fix required for npm to run: `Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned` — same fix pattern as the Python venv activation issue from earlier sessions; needs re-running per new terminal window (not permanent), same as that one.

**New project created:** `trueresearch-frontend` — a **separate folder, sibling to `TrueResearch Code` and `Old Files`** inside `Stock App 2.0` (same separation-of-concerns pattern as Session 4's decision to keep Python pipeline code out of `Old Files`). Created via `npx create-next-app@latest trueresearch-frontend`, using the tool's "recommended defaults" bundle: TypeScript, ESLint, Tailwind CSS, App Router, Turbopack, **no `src/` directory** (the only deviation from the original plan — a cosmetic folder-structure choice, not functionally significant), plus a couple of newer-tool extras (No React Compiler, an `AGENTS.md` file). Automatically initialized its own local Git repository — **separate from** the `truresearch-pipeline` GitHub repo; not yet connected to a GitHub remote or pushed anywhere — this is still local-only, needs backing up once we reach that point.

**Confirmed working:** `npm run dev` starts the local dev server successfully (Next.js 16.3.4, Turbopack), and the default starter page loads correctly at `http://localhost:3000` in the browser. This confirms the full toolchain (Node → npm → Next.js → browser) works end to end before any real content was added.

**Next immediate steps (in order, per the plan agreed with Avdhoot):** (1) connect the frontend to the live Supabase database (same one the Python pipeline writes into) — install `@supabase/supabase-js`, set up environment variables, confirm a real data read works; (2) build the two site-wide components Wireframes.md flags as needed before any individual page (Disclaimer Bar, Learn Callout) plus basic layout/nav; (3) build pages in order: Home, then Stock Detail Page (most important), then Screener, then the rest; (4) deploy to Vercel for a real public URL, likely once a few pages exist rather than waiting for full completion.


## Session 9 — newsletter cadence + PWA reconfirmation
Closes the remaining 2 of 3 items on `PRD.md`'s "Immediate open decisions" list (Mutual Funds, the 3rd, was resolved earlier this same session — see below).

**Newsletter cadence — DECIDED: average 2x/week, SEO-driven.** Replaces the original "daily" framing (which the PRD itself flagged as unrealistic) and the "3-5x/week" range it suggested as a fallback — Avdhoot picked a specific number rather than leaving it a range. Two new requirements beyond just picking a cadence: (1) **SEO-driven** is now an explicit spec, not just a description — every article needs a defined target keyword before writing starts, correct on-page structure (H1/H2 hierarchy, meta description, image alt text), and links out to whichever stock/sector/glossary pages it mentions (reusing the site-wide "every mention is a link" rule already locked in elsewhere). (2) Still text + embedded images per article, video/story formats still deferred. `PRD.md` Section G2 rewritten to match.

**PWA — RECONFIRMED, no override.** Avdhoot explicitly confirmed he's fine with the existing PWA-not-native decision (`PRD.md` K3) now that he's deeper into the project — this was a formality check, not new analysis; nothing changes.

**`PRD.md` updated:** G2 rewritten with the cadence + SEO spec; closing "Immediate open decisions" list — all 3 items now marked resolved.

**Next immediate step:** with all 3 PRD open decisions resolved, decide Phase C's own next build focus — leading candidates remain frontend build (Next.js/React) and scaling qualitative research content to the new 300 stocks.

## Session 9 — Mutual Funds scope decision
**Quick disambiguation (easy to blur these two together — read this before touching either page):** the **Stock Detail Page's** "mutual fund exposure" section is a **Placeholder Panel** ("coming soon" — needs holdings data we don't have). The **Sector Page's** "top mutual funds" section is NOT a placeholder — it's a **live, working category-based approximation** (funds whose AMFI category matches the sector, ranked by AUM), just honestly labeled as category-based rather than holdings-based. Two different pages, two different treatments, for the reason explained below.

Avdhoot picked this as the next open item to resolve (from the "Immediate open decisions" list in `PRD.md`): whether Mutual Funds (J3), pulled up to L1 during scoping and flagged as "comparable in size to the entire original equity Phase 1," should extend the timeline or be held back post-launch.

**Investigated the actual data landscape first, before deciding:** NAV history is fully free (AMFI publishes it officially; free wrapper APIs like MFapi.in expose it with no key needed) and category classification comes bundled with it. Expense ratio and AUM are also free via a second API (ISIN-keyed, e.g. captnemo.in-style). **Portfolio holdings (which stocks a fund holds) is the one genuinely hard piece** — India has no free, structured, cross-fund holdings API; it only exists as monthly PDF factsheets per fund house (AMC), same category of problem as the Screener.in-for-fundamentals decision already made for equities.

**Decision:** Mutual Funds stays in L1, but its scope is narrowed to **NAV + category + expense ratio + AUM + a category-relative score** only. Portfolio-holdings overlap is explicitly deferred to L2 (already tracked as C6, Mutual Fund Overlap Detector) — this removes the one piece that actually justified the "as big as equity Phase 1" concern, so Mutual Funds no longer needs the timeline-extend-or-hold-back trade-off the original PRD flagged.

**Documents updated to match (avoiding promising something not being built):**
- `PRD.md`: Section 0 timeline flag marked resolved; J3 rewritten with final scope + data sourcing plan; B1 (stock detail page) — "exposure across mutual funds that hold it" removed from L1, becomes a placeholder; A3 (sector pages) — "top mutual funds exposed to sector" downgraded to a category-based approximation, explicitly labeled as such (not true holdings-based); closing "Immediate open decisions" list updated.
- `Wireframes.md`: Stock Detail Page's "Mutual fund exposure" block is now a Placeholder Panel, not a live section; Sector Page's fund list relabeled "investing in this space" (category-based, not holdings-based) with a note explaining why; **new Mutual Fund Detail Page wireframe added**, structured to mirror the Stock Detail Page (reuses Score Badge, Asset Card, Disclaimer Bar components — same pattern that already proved out for Gold/J2); Screener wireframe gets a new asset-type toggle (Equities/Mutual Funds/Gold).
- `Database_Schema.md` — **not yet updated this session** (not part of this chat's uploaded files); flag to update its Mutual Fund tables description to match this narrowed scope before Phase C's frontend or data-pipeline build reaches Mutual Funds.

**Next immediate step:** two other "Immediate open decisions" items remain open (newsletter cadence — daily vs. 3-5x/week; confirming PWA as final, though that one's likely just a formality). Separately, decide Phase C's own next build focus: frontend build (Next.js/React) is still the leading candidate, alongside scaling qualitative research content to the new 300 stocks.

## Session 9 — sector-neutral robustness check
Avdhoot chose this as Phase C's next step (over frontend build, scaling qualitative content to the new 300 stocks, or deciding the Mutual Funds timeline). This closes an item that had been open since Session 4/6: "should happen before ever expanding past 200 stocks" — which had, by Session 8, already happened, making this overdue.

**What it checked, in plain terms:** the already-validated `truescore_ml_v2` model's walk-forward Rank IC (0.150 avg, signed off in Session 6) was measured with all stocks pooled together each quarter. That alone can't rule out the model just riding sector-level performance (e.g. always favoring one hot sector) rather than genuinely telling good stocks apart from bad ones within a sector. New script `21_sector_neutral_robustness_check.py` re-ran the exact same validated methodology (same features, same 2-year rolling training window, same XGBoost settings, same `training_dataset.csv`) but additionally measured, per quarter: (1) Rank IC computed separately within each sector then averaged, and (2) whether any single sector persistently dominates the model's top-10%-ranked picks beyond its actual share of the universe.

**Result: PASSED, cleanly.** Across 35 tested quarters (2017 Q4 – 2026 Q2): average pooled Rank IC 0.114, average sector-neutral Rank IC 0.099 — a small gap (0.015) — with 30 of 35 quarters still positive on a sector-neutral basis. No sector was found to persistently over-represent the model's top decile. Reading: the model's edge looks like genuine stock-picking within sectors, not a disguised sector bet.

**Scope note:** this check ran against the existing 200-stock `training_dataset.csv` (the same one used for the signed-off Session 6 validation) — apples-to-apples with that existing evidence. It has NOT yet been re-run against the full Nifty 500 (that would require first rebuilding `training_dataset.csv` via `09_build_training_data.py` against all 500 stocks, a separate, longer-running task, not done this session).

**New files (in `TrueResearch Code`):** `21_sector_neutral_robustness_check.py` (the check itself, read-only — does not retrain or change the live model), `22_save_sector_neutral_results.py` (saves the evidence into `score_backtest_results` under the existing `truescore_ml_v2` formula_version, new `metric_type = "sector_neutral_rank_ic"` — no new formula version registered, since nothing was retrained). Also generated (not hand-written): `sector_neutral_results.csv`, `sector_concentration_detail.csv`.

**Not yet run:** `22_save_sector_neutral_results.py` — needs to be run once to persist this evidence into Supabase (same pattern as `13_save_backtest_results.py`). Do this early next session if not already done.

**Next immediate step:** decide Phase C's next focus. Frontend build (Next.js/React) remains the most likely candidate per Session 8's own note — real, now-validated-on-two-fronts TrueScore data exists across the full Nifty 500 to build against. Other candidates still open: scaling qualitative research content (reports, sector overviews, peer lists, etc.) to the new 300 stocks (flagged in Session 8 as not yet done), and confirming whether the Mutual Funds L1-timeline decision (PRD Section 0) was ever formally recorded.

## Session 8 — scaling to Nifty 500
Avdhoot's explicit call: the ML model itself is NOT retrained or re-validated as part of this — `trueresearch_model.json` / `formula_version truescore_v2` stays exactly as signed off in Session 6. This push only expands how many stocks that same, already-validated model gets applied to (200 → 500), since `14_score_current_stocks.py` was already built to loop over "every active equity," not a fixed list — no code change was needed there at all.

**What was done, in order:**
1. Sourced the current, official Nifty 500 constituent list directly from NSE's own index archive (not a stale third-party mirror — cross-checked against very recent listings like Ather Energy, Groww, FirstCry to confirm currency). Saved as `nifty500_master.csv`.
2. Compared it against the database's existing 200 tickers (via new one-off helper `18_export_current_tickers.py`) — clean diff: exactly 300 missing, and (sanity check) 0 of the existing 200 were found to be stale/non-current. Saved the 300 as `missing_300_nifty500.csv`.
3. `19_add_nifty500_expansion.py` added all 300 to `assets` (asset_type=equity, is_active=true), plus 2 new sector rows the existing 18 didn't cover (`Diversified`, `Media Entertainment & Publication`). Ran clean: 300/300.
4. `20_backfill_new_stocks_price_history.py` — same 10-year backfill logic as the original Phase B `08_backfill_price_history.py`, but skips any stock that already has price history, so it only did real work for the new 300 (and stays safe/reusable for any future additions too). Ran clean: 300/300.
5. Re-ran the existing `06_weekly_fundamentals_refresh.py` (loops over all active equities, so it picked up the new 300 automatically, no code change) — ran clean: 500/500.
6. Re-ran the existing `14_score_current_stocks.py` — same model, same `formula_version truescore_v2`, no retraining. Result: **498 of 500 scored** (2 skipped — ICICIAMC and MEESHO, both too newly listed to have the ~200 trading days of price history the model's indicators need — expected, not a bug), run_date 2026-09-06.

**New files (in `TrueResearch Code`):** `nifty500_master.csv`, `18_export_current_tickers.py`, `missing_300_nifty500.csv`, `19_add_nifty500_expansion.py`, `20_backfill_new_stocks_price_history.py`.

**What did NOT scale to 500 (flagged deliberately, not silently skipped):** written research reports, sector overview text, "why it's here" one-liners, peer lists, management profiles, insider transaction history, shareholding-pattern history — these were manually curated for the original 200 (PRD Section B3 territory) and are separate, ongoing research work, not part of this automated data-scaling push. The 300 new stocks have full automated data (prices, fundamentals, ratios, TrueScore) but not this qualitative content yet.

**Also now automatically covered for all 500, no extra work needed:** the daily price refresh, weekly fundamentals refresh, and the Session 7 performance-tracking job all loop over "every active equity" with no filtering by which batch a stock came from — so the new 300 are already inside all three scheduled GitHub Actions jobs going forward.

**Open item carried forward (unchanged from Session 4/6):** the "sector-neutral" robustness check for the ML model has still never been run — Avdhoot's explicit call this session was to leave the model untouched and revisit it later, so this remains deliberately deferred, not silently dropped.

**Pushed to GitHub and confirmed** (all 5 new files, commit `d0643d4`). This file (`PROJECT_STATE.md`) itself had a hiccup: an earlier attempt to save this Session 8 update didn't actually reach disk (silently), so it was missing from that same commit — caught and re-saved properly at the start of the next session; if you're reading this, the fix worked.

**Next immediate step:** decide Phase C's next focus — likely the frontend build (Next.js/React), now that all 500 stocks have real, live TrueScore data to build against.

## Session 7 — Phase C begins: ongoing TrueScore performance tracking
Avdhoot chose this as Phase C's first focus (over frontend build or scaling to Nifty 500): the `score_performance_tracking` table PROJECT_STATE.md had flagged as "not yet set up, good candidate for early Phase C" now has a real, automated job behind it.

**What it does, in plain terms:** every score TrueResearch has ever handed out (any `run_date`/`formula_version` combination already in the `scores` table) gets grouped into 10 "deciles" by that day's `overall_score` (decile 10 = highest-scoring stocks that day, decile 1 = lowest). For each of three forward windows — 1 month, 3 months, 1 year — the job checks whether enough real time has actually passed since that score was given; if it has, it computes each stock's real price return over that window and averages it within its decile, then saves one row per (date, formula_version, decile) into `score_performance_tracking`. This is the living, ongoing version of the one-time `TrueScore_Validation_Report.md` backtest — it keeps accumulating real evidence forever instead of going stale.

**New files (in `TrueResearch Code`):**
- `16_add_performance_tracking_run_type.sql` — one-time migration; adds `'performance_tracking'` as a 4th allowed value in `ingestion_runs.run_type` (previously only daily_prices/weekly_fundamentals/scoring), so this job can log its runs the same way the others do.
- `17_score_performance_tracking.py` — the job itself. Safe to re-run any time (upserts on `date, formula_version, decile`); logs every run into `ingestion_runs` via `ingestion_log.py`, same as the other pipeline jobs.
- `.github/workflows/score-performance-tracking.yml` — runs it automatically every Sunday (3am UTC, an hour after the existing weekly fundamentals job), plus a manual "Run workflow" button, same pattern as the other two scheduled jobs.

**Honest expectation to set, and this matters:** because every score currently in the database (`legacy_interim_v1`, `truescore_v1`, `truescore_v2`) was generated within the last couple of sessions, running this job today finds **nothing to write yet** — not even the 1-month window has passed for any of them. That is correct, expected behavior, not a bug. Real rows start appearing automatically, a few at a time, as the weeks and months pass and the Sunday job keeps running — the 1-month numbers for today's scores will be the first real evidence, appearing roughly 30 days from today (2026-09-06).

**Not pushed to GitHub yet** — same open item as Session 4/5's pattern: these 3 new files exist locally in `TrueResearch Code` but need a `git add . && git commit -m "..." && git push` to reach the repo and let the new scheduled workflow actually start running on GitHub's servers (a workflow file only takes effect once it's pushed to the repo's default branch).

**Next immediate step:** push these files to GitHub (see instructions given in-chat this session), confirm the new workflow appears and runs successfully via a manual "Run workflow" test (it will report "0 rows written" the first time — that's the expected empty-first-run behavior described above, not a failure), then decide Phase C's next focus (frontend build, or scaling toward Nifty 500).

## Session 6 — Step 6 correction (IMPORTANT, read before touching TrueScore)
A review comparing Session 5's Step 6 work against the actual, previously-validated "Case C" model from Avdhoot's separate ClearStocks ML experiment found THREE real problems:
1. **Feature drift.** Session 5's model (`11_train_model.py`, `12_walkforward_validation.py`, `14_score_current_stocks.py`) trained on 17 numeric features, but the original StockApp experiment's validated Case C model used exactly 14 — three extra columns (`Debt_to_Equity`, `Revenue_Growth_YoY`, `NetIncome_Growth_YoY`) had been quietly added and were never independently tested. **Fixed:** all three scripts now use the exact validated 14-feature list.
2. **Live-scoring bug.** `14_score_current_stocks.py`'s `Relative_Strength_3M` input just reused the stock's raw `Return_3M` instead of comparing it to the Nifty 100 benchmark's own 3-month return — the training script computed this correctly, but live scoring didn't match it. **Fixed:** live scoring now fetches the benchmark and computes it the same way training does.
3. **Dataset-building bug (found after Avdhoot asked why only 10 quarters were validated vs. the original's 13).** `09_build_training_data.py` was SKIPPING any snapshot date entirely when a stock's fundamentals weren't usable yet at that point in time. Row-by-row comparison against the original `training_dataset.csv` showed the original KEPT those early "technical-only" snapshots (price/momentum data present, fundamentals columns just blank/NaN) all the way back to August 2022, and let XGBoost handle the missing values natively (documented as intentional in the original `train_model.py`'s own comments). Skipping them cut 2022 Q3–2023 Q2 from 900+ rows down to about 70, which is why walk-forward validation could only start at 2024 Q1 instead of the original's 2023 Q2 — three quarters of already-proven validation history lost for no real reason. **Fixed:** `09_build_training_data.py` now keeps technical-only rows with blank fundamentals instead of dropping them.

**Honest nuance flagged to Avdhoot (not a bug, just worth knowing):** in the stretch where almost every stock's fundamentals are simultaneously blank (~Aug 2022–mid 2023), the model is effectively running as technical-only for those quarters, not the full technical+fundamentals blend it becomes later. This is a property of the original Case C results too (whether or not it was called out explicitly at the time), not something the fix introduces. Worth a quick before/after look on early vs. later quarters once the dataset is rebuilt, though not blocking.

**What was done in this session so far:** `11_train_model.py` and `12_walkforward_validation.py` were re-run locally against the (pre-fix-#3) `training_dataset.csv` with the corrected feature list from fixes #1/#2 — that intermediate result (6 of 10 quarters significant, avg Rank IC 0.101, avg spread 6.0pp) is SUPERSEDED and will change again once the dataset is rebuilt with fix #3. Do not treat those numbers as final. Also researched (2026, current) free/paid options for deeper Indian fundamentals history beyond yfinance's ~4-year free window — conclusion: no clean free 10-year option exists (Screener.in is the best value at ~₹5k/yr but needs a paid plan for programmatic export; NSE/BSE filings are free but require heavy scraping; global APIs like Alpha Vantage/FMP have weak-to-absent India coverage). See "Planned for Phase 5" below for Avdhoot's decision on this.

**Part 3 update — dataset rebuilt, and a 4th finding led to a locked-in methodology change.** Avdhoot ran the corrected `09_build_training_data.py` locally: it built 18,755 rows (up from 6,475), but spanning back to **2017 Q2** instead of just 2022 — because TrueResearch's price history already had a full 10-year backfill (from Session 5) that the row-dropping bug had been hiding the benefit of. Re-running the walk-forward validation on this fuller dataset with the SAME "expanding window" approach as before actually made recent-quarter (2024-2026) results WORSE (avg Rank IC fell to 0.061) — training on 2017-2021 data (very different market regimes, including COVID) was diluting what the model learned about today's market. Five different training-window strategies were tested (see `Step6_ML_Model_Decision_Log.md` and `experiment_window_comparison.py` for full detail); **a 2-year rolling training window won clearly** and is now locked in as the final methodology (`formula_version truescore_ml_v2` / combined `truescore_v2`).

**Final validated result, on the exact 13-quarter window matching the original StockApp Case C experiment (2023 Q2 – 2026 Q2):** 11 of 13 quarters directionally positive, 9 of 13 statistically significant, average Rank IC **0.150**, average spread **12.5 points** — this now BEATS the original's own result (12/13, avg IC ~0.133) on real, current TrueResearch data. Full quarter-by-quarter numbers for all 37 tested quarters (2017-2026) are in `walkforward_results.csv`.

**All production files updated and regenerated with the final methodology:** `09_build_training_data.py`, `11_train_model.py`, `12_walkforward_validation.py`, `13_save_backtest_results.py`, `14_score_current_stocks.py`, `trueresearch_model.json`, `model_feature_columns.json`, `walkforward_results.csv`, `TrueScore_Validation_Report.md` (rewritten as the final sign-off doc). New: `Step6_ML_Model_Decision_Log.md` — a complete chronological record of all 3 bugs, the rolling-window experiment and its numbers, and every honest caveat, kept specifically so this model can be revisited later without re-deriving any of it. Also new (reference only, not part of the production pipeline): `experiment_window_comparison.py`, the script used to test the 5 training-window variants.

**CLOSED OUT (Session 6, part 4).** Avdhoot reviewed `TrueScore_Validation_Report.md` and signed off. Both remaining scripts were run locally:
- `13_save_backtest_results.py` — saved the final walk-forward evidence under formula_version `truescore_ml_v2` to `score_backtest_results`. All prior versions (`truescore_ml_v1`, `truescore_ml_v1_corrected`) remain in the database untouched.
- `14_score_current_stocks.py` — re-scored stocks under formula_version `truescore_v2`. Result: **199 of 200 stocks scored** (1 skipped, same as prior runs — expected, not enough usable data yet), run_date 2026-09-06. Top-ranked stocks this run: IDEA, NTPC, LUPIN, NMDC, IOC, SUZLON, BANKINDIA, PFC, ONGC, PNB — all rated "Strong." All prior score versions (`truescore_v1`, `truescore_v1_corrected`) remain in the database untouched, so before/after comparison is always possible.

**Phase B Step 6 is now genuinely complete** — correctly matching (and on a fair comparison, beating) the originally validated Case C methodology, with the full story preserved in `Step6_ML_Model_Decision_Log.md` for future reference.

## Planned for Phase 5 (Avdhoot's explicit call, Session 6)
Once Phase 5 (deeper research/content work) is underway, retrain TrueScore's ML model using a full ~10 years of Indian company fundamentals sourced from **Screener.in** (their ~₹5k/year "Active Investor" plan gives 10-year financial data + Excel export, the best-value option found — see research above; free sources either don't go back far enough (yfinance, ~4 years) or require heavy scraping engineering (NSE/BSE XBRL filings) for the same result). This would let the model train on real 10-year financials-with-numbers everywhere, rather than the current approach of leaving fundamentals blank for dates before 2023. Not started yet — flagged here so it doesn't get lost before Phase 5 planning.

**Open item carried forward:** the original StockApp experiment's "sector-neutral" robustness check (proving the model's edge wasn't just from favoring one hot sector in a given quarter) has still never been replicated in TrueResearch. Not urgent, but should happen before ever expanding past 200 stocks.

## Where we are
**Phase A is fully done. Phase B (Foundation) is now fully done too** — see the 6-step checklist below, all ✅. Existing 199-stock static site ("Old Files" folder, old name "ClearStocks") is untouched and still live — nothing has been migrated into it yet. All new Phase B code/scripts/docs were written into a **separate folder, `TrueResearch Code`**, on Avdhoot's computer (sibling to `Old Files`). Phase C (likely the actual frontend) has not started yet.

**Phase B progress:**
1. ✅ Supabase account + project created (project name: `TrueResearch-Prod`, region: South Asia/Mumbai, free tier)
2. ✅ Database schema fully built — ~39 tables created and confirmed in Table Editor, Row Level Security enabled on all tables (locked down by default, no public access until policies are written later). Full schema documented in `Database_Schema.md`; the exact SQL that created it is in `01_create_tables.sql`. Both live in the `TrueResearch Code` folder.
3. ✅ Data provider abstraction layer built and tested. `market_data_provider.py` is the ONLY file allowed to call `yfinance` directly (per locked tech-stack rule); `db_client.py` is the ONLY file that connects to Supabase directly. Local Python setup: `venv` virtual environment created in `TrueResearch Code`, packages installed (`yfinance`, `pandas`, `supabase`, `python-dotenv`), `.env` holds the Supabase Project URL + secret API key (never committed to GitHub — `.gitignore` covers `.env` and `venv/`). `test_setup.py` confirms both Yahoo Finance and Supabase connections work — last run: both SUCCESS. One extra step was needed beyond the original plan: ran `02_grant_permissions.sql` to explicitly grant `service_role` access to all tables (a direct consequence of deliberately unchecking "Automatically expose new tables" at project setup — expected, not a mistake).
4. ✅ Existing 199-stock data migrated into Supabase. `04_migrate_existing_data.py` (run after `03_add_ratio_columns.sql`, which added market_cap/week52_high/week52_low to `ratios_snapshot`) loaded: 18 sectors, 200 assets, 795 fundamentals rows, 200 ratios_snapshot rows, 200 live_prices rows, and 200 "legacy baseline" score rows. **Important:** those legacy scores are stored under `formula_version = "legacy_interim_v1"` and are explicitly documented (in `score_formula_versions`) as the OLD `compute_auto_scores.py` interim automated score — NOT real TrueScore. Never treat `legacy_interim_v1` rows as validated TrueScore output. The script is safe to re-run (upserts everywhere, no duplicates).
   - **Open item, deliberately not done yet:** the 199 PDF "Company Decks" (in `Old Files/decks/decks/`) were NOT migrated — their content is inside PDF files, not structured data, and extracting text from 199 PDFs is its own task. Revisit when `research_reports` needs real content (PRD B3).
5. ✅ Daily/weekly ingestion pipeline rebuilt and automated. `05_daily_price_refresh.py` (adapts old `fetch_live_prices.py`: live price + day change into `live_prices`, PLUS a new addition — also appends today's OHLCV bar into `prices_daily`, which is what slowly builds real price history going forward, on top of the still-pending one-time 5-year backfill) and `06_weekly_fundamentals_refresh.py` (adapts old `refresh_weekly_data.py`: full annual financial statements into `fundamentals`, valuation ratios into `ratios_snapshot` — same defensive logic preserved: real NaN vs missing distinguished, a stock's existing data is never wiped on a failed fetch, only skipped and logged) both write into Supabase via `db_client.py`/`market_data_provider.py`, and both log every run into `ingestion_runs` via the new `ingestion_log.py` helper (replaces `status.json`). Manually tested successfully (200/200 both jobs). **Now automated via GitHub Actions**, not just manual: code pushed to a new private GitHub repo (`https://github.com/sp2591996/truresearch-pipeline`), Supabase credentials stored as GitHub Actions secrets (`SUPABASE_URL`, `SUPABASE_SECRET_KEY` — never in code), two scheduled workflows (`.github/workflows/daily-prices.yml` every ~15 min during NSE market hours, `.github/workflows/weekly-fundamentals.yml` Sunday mornings) — BOTH confirmed working via manual "Run workflow" test runs: daily = Success (~3.5 min), weekly = Success (~8 min). A "Node.js 20 deprecated" annotation on both runs is a routine GitHub infrastructure notice, not an error.
6. ✅ TrueScore back-test/validation COMPLETE. Model built, trained, walk-forward validated, signed off by Avdhoot (see `TrueScore_Validation_Report.md`), and real TrueScore + TrueScore Rating saved for all 200 stocks (`formula_version = "truescore_v1"`, run_date 2026-09-06). **Phase B is now fully complete** — all 6 foundation steps done. Details below.

## Session 5 changes (Phase B, Step 6 — TrueScore ML model)
- **Scope decision (Avdhoot's explicit call):** rather than reuse the pre-existing trained model/evidence from a separate experimental project (`C:\Users\Komal\Desktop\StockApp`), the training dataset was rebuilt and the model retrained entirely from TrueResearch's own Supabase data, faithfully replicating that project's methodology (same indicator formulas, same point-in-time-correct fundamentals logic, same XGBoost settings). The old project's trained model file was NOT reused.
- **Backfill correction:** the one-time historical price backfill (flagged back in Session 4 as "5-year") was done as **10 years** instead — this matches the original experimental project's own reasoning (10 years gives enough runway before the 2020 COVID crash to have a meaningful lead-in, not just start right at the edge of it). `07_add_index_asset_type.sql` added "index" as a valid `asset_type` so the Nifty 100 benchmark could be stored as a normal `assets` row; `08_backfill_price_history.py` did the backfill for the benchmark + all 200 stocks (200/200 succeeded).
- **Training data built ourselves:** `09_build_training_data.py` reads directly from our `prices_daily` and `fundamentals` tables (not old CSV files) and builds point-in-time-correct snapshots (a fundamentals row only counts once its fiscal-year-end + 120 days has passed, matching real-world reporting lag). Result: 6,475 training rows across 200 stocks. **Bug fixed during this process:** Supabase caps each read at 1,000 rows regardless of `.limit()` — the first version of this script silently lost all price history after the first 1,000 daily rows for long-listed stocks (making established companies like Reliance/TCS produce zero training rows). Fixed by paginating reads with `.range()`. Any FUTURE script that reads more than ~1,000 rows from any table must paginate the same way — this is a recurring gotcha, not a one-off.
- **Model trained:** `11_train_model.py` — XGBoost regression (200 trees, depth 4, learning_rate 0.05, random_state 42), predicting 3-month excess return vs. Nifty 100, same hyperparameters as the original experimental project. **Deliberately excludes macro-economic features** (USD/INR, oil, gold, repo rate) — no ingestion pipeline exists for that data yet in TrueResearch; flagged as a possible future enhancement, not an oversight.
- **Properly walk-forward validated:** `12_walkforward_validation.py` retrains per calendar quarter using only data available at that time, tests on the next quarter. Result: 7 of 10 quarters statistically significant and positive, 8 of 10 directionally positive, average Rank IC 0.115, average top-vs-bottom decile spread 8.5 percentage points. Full detail and honest caveats (short 2.5-year test history vs. the original project's longer COVID-inclusive history; two weak quarters in mid-to-late 2024) are in `TrueScore_Validation_Report.md`.
- **Evidence saved permanently:** `13_save_backtest_results.py` registered a new `score_formula_versions` row (`truescore_ml_v1`) and saved all 10 quarters' Rank IC + decile-spread results into `score_backtest_results` — this is literally the database table the PRD requires as the "how successful has TrueScore been" record, and the validation gate PROJECT_STATE.md requires before any coverage expansion.
- **Avdhoot reviewed `TrueScore_Validation_Report.md` and signed off.** `14_score_current_stocks.py` then scored all 200 stocks and saved results to the live database: `scores` (both `overall_score` — the 0-100 TrueScore number — AND `truescore_rating` — the plain-English label: Strong/Above Average/Average/Below Average/Weak, derived FROM that number, not a separate independent judgment) and `score_components` (the 2 underlying factors per stock: relative_valuation, ml_rank), under `formula_version = "truescore_v1"` (the combined formula — distinct from `truescore_ml_v1`, which specifically tags the ML sub-model's own validated version). Default component weighting (50% relative valuation / 50% ML rank) is stored as DATA in `score_component_weights` (universe-wide default, `sector_id`/`asset_id` both NULL) — not hardcoded, so it can be tuned later per sector/stock without a code change. Ran successfully: 199 of 200 stocks scored (1 skipped — not enough usable data yet, expected and normal). Top-ranked stocks as of this first run: IDEA, IOC, CANBK, HUDCO, BANKINDIA and others — all "Strong" rated.
  - **Bug fixed along the way:** `scores.truescore_rating` had accidentally been created as a `numeric` column back when the schema was first built (it needs to hold text labels like "Average"). Fixed with `15_fix_truescore_rating_type.sql` (changed the column to `text` — safe, since the column held no real data yet).
- New files in `TrueResearch Code` this session: `07_add_index_asset_type.sql`, `08_backfill_price_history.py`, `09_build_training_data.py`, `10_diagnose_fundamentals.py` (one-time diagnostic, kept for reference), `11_train_model.py`, `12_walkforward_validation.py`, `13_save_backtest_results.py`, `14_score_current_stocks.py`, `15_fix_truescore_rating_type.sql`, `TrueScore_Validation_Report.md`, plus generated (not hand-written) `training_dataset.csv`, `trueresearch_model.json`, `model_feature_columns.json`, `walkforward_results.csv`. `requirements.txt` updated to add `xgboost` and `scikit-learn`.

## Local environment reference (Session 4)
- Python 3.12.10 confirmed installed on Avdhoot's computer.
- Project folder: `C:\Users\Komal\Desktop\Stock App 2.0\TrueResearch Code` (opened as a VS Code workspace).
- To resume work in a new terminal session: open a Terminal in VS Code (should default to this folder), then run `.\venv\Scripts\Activate.ps1` to reactivate the virtual environment (look for `(venv)` at the prompt). If PowerShell blocks this with an execution-policy error, run `Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned` first, then retry — this is a per-terminal-session setting, needs re-running each new terminal window (not a one-time fix).
- Files in this folder so far: `Database_Schema.md`, `PROJECT_STATE.md`, `01_create_tables.sql`, `02_grant_permissions.sql`, `03_add_ratio_columns.sql`, `04_migrate_existing_data.py`, `05_daily_price_refresh.py`, `06_weekly_fundamentals_refresh.py`, `db_client.py`, `market_data_provider.py`, `ingestion_log.py`, `test_setup.py`, `requirements.txt`, `.env` (secret, git-ignored), `.gitignore`, `.github/workflows/daily-prices.yml`, `.github/workflows/weekly-fundamentals.yml`, `venv/` (git-ignored).
- **GitHub repo (Session 4):** `https://github.com/sp2591996/truresearch-pipeline` (private). This folder is a git repository tracking that remote — `git add . && git commit -m "..." && git push` from this folder pushes changes. Supabase secrets live in the repo's Settings → Secrets and variables → Actions, never in code.
- **Open item: Session 5's new files (scripts 07-15, `TrueScore_Validation_Report.md`, updated `requirements.txt`) have NOT yet been pushed to GitHub** — they exist locally in `TrueResearch Code` and are fully working, but have not been backed up to the repo the way Session 4's files were. Worth doing a `git add . && git commit -m "Phase B Step 6: TrueScore ML model" && git push` early in the next session, purely as a backup step (this is NOT required for anything to keep working — the daily/weekly GitHub Actions jobs are unrelated and already running fine).

## Session 4 changes
- **Existing data source found:** Avdhoot's prior static site ("ClearStocks," Nifty 200, in `Old Files` folder) already has a working, GitHub-Actions-automated pipeline: `fetch_live_prices.py` (intraday, every 10-15 min via yfinance fast_info), `refresh_weekly_data.py` (heavier weekly fundamentals refresh), `compute_auto_scores.py` (an interim automated sector-relative valuation + ROE score — a precursor to TrueScore, not the real ML model). 199 stocks have `fundamentals_history` CSVs; 199 PDF "Company Decks" also exist. This significantly de-risks Phase B — we're upgrading/relocating a working pipeline into a database, not building ingestion logic from zero.
- **Known gap flagged:** existing data has no daily price *history* (only current price + fundamentals-history CSVs), so a one-time 5-year historical price backfill from Yahoo Finance is planned as part of steps 4/5, not a separate later phase.
- **Schema decisions (all confirmed by Avdhoot):**
  - Tables for Gold/Mutual Funds/Debt/REIT-placeholder/Intl-placeholder are created now (structure only), populated in their own later phases — this is what "sized for" meant in the original Phase B brief.
  - 200→500 stock sequencing: migrate/validate on the existing 200 first, per the locked TrueScore validation rule; the other 300 stocks' full data comes after validation, not before.
  - The persona quiz (PRD A2) and the Risk-Profiling Questionnaire (PRD E4) share one `questionnaire_questions`/`questionnaire_options` table structure, tagged by `questionnaire_type` — Avdhoot's explicit call, to avoid duplicate tables for the same shape of data.
  - **TrueScore composition is NOT hardcoded into fixed columns.** Only the 2 confirmed-known factors (relative valuation, ML rank/score) get dedicated columns in `scores`. Every other factor — current or future, and possibly stock/sector-specific ("to be decided for each stock," Avdhoot's words) — lives in two flexible tables: `score_components` (one row per factor per stock per run) and `score_component_weights` (which factors apply where, and at what weight — a data change, not a schema change). This was a mid-build correction Avdhoot caught before the schema was created; flagged here so it's never accidentally "fixed" back to fixed columns.
  - Added `score_backtest_results` (one-time validation-gate report per PRD B2) and `score_performance_tracking` (ongoing, continuously-updated decile-based forward-return tracking) — both per Avdhoot's explicit ask: "make sure we use that to show how much successful our scores have been." The `scores` table itself is already historical (new row per `run_date`/`formula_version`, never overwritten).
  - Added several tables not explicit in the original PRD text but implied by it, caught during a full line-by-line PRD pass (Avdhoot's request): `curated_list_configs`, `themes`, `market_mood_daily`, `peers`, `sector_ratio_config`, `score_formula_versions`, `research_report_templates`, `management_profiles`, `insider_transactions`, `shareholding_pattern`, `learn_content`, `articles`, `ipos`, `preset_screens`, `saved_comparisons`, `calculator_shares`, `alert_subscriptions` (persists placeholder toggle state even though real alerts aren't built), `mutual_fund_nav_history`, `mutual_fund_holdings`, `debt_reference_rates`, `api_keys`, `api_usage_log`, and two new general-purpose ones: `site_content_blocks` (editable disclaimer/legal copy without a code deploy) and `placeholder_copy` (centralized text for every L2-L4 "coming soon" placeholder, per the cross-cutting placeholder rule).
- **Security default confirmed:** Row Level Security (RLS) enabled on all tables at creation time — tables are inaccessible via the public API until access policies are deliberately written later (matches the earlier "don't auto-expose new tables" choice made during Supabase project setup).

## Locked decisions (carried over, unchanged)
- **Equity scope:** Nifty 500 — LIVE as of Session 8, not just planned. 498 of 500 stocks have real TrueScore data (see "Session 8 — scaling to Nifty 500" above).
- **Other asset classes (MVP):** Gold (full L1, proof-of-concept), Mutual Funds (**pulled up to L1** — timeline trade-off already decided: extend timeline, don't cut scope, see Session 3), Debt (L1 = static reference rates only), REIT (L1 = placeholder only), International Equity (L1 = placeholder only).
- **Mobile:** Progressive Web App — confirmed final for L1, not native. **Reconfirmed by Avdhoot in Session 9** — no override.
- **TrueScore/ML model:** must be back-tested/validated on current 200-stock data BEFORE expanding coverage to 500 or adding auto-retraining. Sequence: validate → expand coverage → automate. TrueScore Rating built alongside core TrueScore but kept visually/structurally distinct. **Sector-neutral robustness check: DONE, PASSED (Session 9)** — model's edge holds up within sectors, not just pooled across the universe; no sector persistently dominates top picks. Still open: re-running this same check against the full 500-stock universe (currently only checked against the original 200), and the Screener.in 10-year-data retrain planned for Phase 5.
- **Education content rule:** every educational point paired with a picture/graph/graphic/flowchart — never text alone.
- **Newsletter/content:** on-site, **SEO-driven** articles + embedded images, **average 2x/week** (decided Session 9, replaces the earlier "daily"/"3-5x/week" range — see below).
- **Placeholders:** every "coming soon" must say what's coming — now also backed by the `placeholder_copy` table (Session 4).
- **Build sequencing:** PRD → Design system + wireframes → User journey maps → Code (data pipeline first, then frontend). *(Phase A complete. Phase B in progress — see "Where we are" above.)*
- **Branding — FULLY DECIDED:** TrueResearch, Logo Concept B ("TR Monogram" shape). TrueScore / TrueScore Rating naming applied everywhere. **Color palette changed in Session 10, part 2** — no longer Navy & Gold; now "Deep Space & Violet," matched from the old ClearStocks site. See `Design_System.md` Section 2b.
- **Login:** Email + Google via Supabase Auth.
- **Regulatory framing:** all scores/research = "research signal," never "advice."
- **Screener/comparison, Portfolio, Calculators, IPO Tracker, Alerting infrastructure:** all as decided in Session 3 (see prior version of this file / `PRD.md` for full detail — unchanged in Session 4).

## Tech stack (unchanged)
Next.js + React + TypeScript, Tailwind CSS, Supabase/Postgres (**now live**, project `TrueResearch-Prod`, region ap-south-1/Mumbai), Python for ingestion/scoring, GitHub Actions for automation, Vercel hosting. Data provider abstraction layer (never call yfinance directly from business logic) — next step. All free-tier, $0/month + domain cost through Phase 6.

## Feature scope reference
`Feature_Universe_Scope_v2_Response.xlsx` — source of truth for in/out of scope per phase. `PRD.md` — buildable-spec translation. **`Database_Schema.md` (Session 4) is now the buildable-spec translation of the PRD into actual data structures** — read it for "what table holds this," read PRD.md for "what does this feature do."

## Open/pending items
- **TEMPORARY PROVISION — flagged for L2 update (Session 9):** the two mutual-fund-holdings stand-ins (Stock Detail Page's "coming soon" Placeholder Panel, and Sector Page's category-based-approximation fund list) are NOT permanent designs — they exist only because holdings data doesn't exist yet at L1. The moment C6 (Mutual Fund Overlap Detector) ships with real holdings data, BOTH need to be swapped for the real thing: the stock-page placeholder becomes a live "held by these N funds" list, and the sector-page category-approximation gets upgraded to true holdings-based "funds actually holding stocks in this sector." The database is already prepared for this — `mutual_fund_holdings` table was created back in Session 4 (structure only, per the "sized for" note above), so this is a data-population + UI-logic swap when the time comes, not a schema change. Do not silently forget this swap once C6 is built — check this note.
- Exact revised timeline (dates) for Phase B–F — still not worked out; will become clearer once ingestion pipeline (steps 3-5) is built and real build velocity is known.
- Legal/regulatory review — deferred to pre-commercial-launch.
- **Phase B is DONE.** All 6 foundation steps complete: schema, data provider layer, migration, automated daily/weekly ingestion, and now a validated, signed-off, live TrueScore for all 200 stocks.
- **Set up in Session 7 (Phase C, Step 1):** `score_performance_tracking` — the ONGOING, continuously-updated version of validation is now built and automated (weekly, Sundays 3am UTC via GitHub Actions), per the plan flagged here at the end of Phase B. See "Session 7 — Phase C begins" above for full detail. Real numbers will start appearing automatically over the coming weeks/months as scores age past 1 month/3 months/1 year.
- **Set up in Session 8 (Phase C, Step 2):** scaled from 200 to the full Nifty 500 — 498 of 500 stocks now have live TrueScore, model untouched (no retraining), all 3 scheduled jobs already cover all 500 automatically. See "Session 8 — scaling to Nifty 500" above for full detail.
- **Set up in Session 9 (Phase C, Step 3):** sector-neutral robustness check on the TrueScore ML model — PASSED. Closes an item open since Session 4/6. See "Session 9 — sector-neutral robustness check" above for full detail. **`22_save_sector_neutral_results.py` still needs to be run** to persist this evidence into Supabase.
- **Set up in Session 10 (Phase C, Step 5):** the two site-wide components (Disclaimer Bar, Learn Callout), plus Nav/Footer/layout and a real Home page reading live TrueScore data — built first in Navy & Gold, then restyled the same session to match the old ClearStocks site's dark violet look (now the locked palette). See "Session 10, part 1" and "Session 10, part 2" above for full detail.
- **Next immediate step:** build the **Stock Detail Page** next (PRD.md B1 / Wireframes.md — the most important single page), reusing the (now-restyled) components built in Session 10, then Screener. New chat should open with: "Read PROJECT_STATE.md, continuing TrueResearch — Phase C, Home page/site-wide components/rebrand are done, let's build the Stock Detail Page."

## Working protocol
- One `PROJECT_STATE.md` update at the end of each work session.
- New chat → say "read PROJECT_STATE.md, continuing [project], we're on [step]" → attach/reference this file.
- I flag every new idea raised mid-build as either "same session" or "later phase," with reasoning — never silently deferred or silently accepted.
- Token efficiency: reference this file instead of re-explaining locked decisions.
- **User is a non-coder and needs extreme hand-holding for every execution step** — including multi-step code execution. Never assume familiarity with terminals, git, file paths, or dev tools. Every instruction spells out exactly which button/menu/field, in order, with no skipped steps.
- **Any time a script is updated (not just created new), the ENTIRE revised script is given in full** (never a diff/snippet/"add this line"), plus explicit plain-language instructions on exactly what to do with it (which file to replace, where it lives, how to run it, what output to expect).
- **New (Session 4):** all new Phase B code/scripts/docs are written into the `TrueResearch Code` folder on Avdhoot's computer (a sibling folder to `Old Files`, both inside `Stock App 2.0`) — never into `Old Files`, which stays untouched as the live old site until migration is complete.
